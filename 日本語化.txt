OSをインストールして、日本語が通っていない場合
以下のコマンドを実行することで、日本語入力が可能になります。

sudo raspi-config nonint do_change_locale ja_JP.UTF-8
sudo raspi-config nonint do_change_timezone Asia/Tokyo
sudo apt install fcitx5-mozc -y
im-config -n fcitx5
